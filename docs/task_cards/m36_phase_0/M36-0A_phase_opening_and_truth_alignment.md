# M36-0A Phase Opening And Truth Alignment

Status: completed

## Goal

Open `M36 Phase 0` formally and align repository truth so the next bounded phase is described honestly before deeper `M36` implementation continues.

## Acceptance

- add the `M36 Phase 0` phase doc
- add the `M36 Phase 0` task-card index and detailed cards
- update the workflow guide so accepted `M36 Phase 0` becomes the latest bounded freeze
- update README, root planning docs, and the roadmap so repository truth points at accepted `M36 Phase 0`
- record that `M35` remains the latest completed milestone and `M36 Phase 0` is only the bounded opening/freeze slice
- preserve `TD-STRUCT-005` and `TD-STRUCT-006` as deferred
- freeze the default workflow-orchestration model for `M36 Phase 0`: workspace-scoped DB, interaction-session kickoff, task-card-to-run mapping, explicit mutation contracts, explicit review gates, and evidence writeback

## Notes

- do not start `M36 Phase 1` conversational workbench implementation under `M36-0A`
- do not generate `M36 Phase 1` or `M36 Phase 2` task-card packs yet
- workflow remains the bounded execution and evidence plane; final milestone judgment remains human-led

## Result

- opened `M36 Phase 0` formally through the phase doc and task-card pack
- updated the workflow guide, README, root roadmap pointer, and rebuilt roadmap so the repository now points at accepted `M36 Phase 0`
- preserved `M35` as the latest completed milestone while making `M36 Phase 0` the latest accepted bounded freeze
- froze the workflow-driven execution model for the rest of `M36`
- kept `TD-STRUCT-005` and `TD-STRUCT-006` explicitly deferred
