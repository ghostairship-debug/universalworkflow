# M8 Phase 6 Task Cards

| Task | Size | Goal | Primary Files | Done When |
| --- | --- | --- | --- | --- |
| `M8-6A` | `medium` | Add direct M8 tests | `tests/test_execution_loop.py`, `tests/test_api.py`, `tests/test_cli.py` | M8 feature lanes have explicit coverage |
| `M8-6B` | `small` | Repair editable-install packaging | `pyproject.toml` | `pip install -e . --no-deps` succeeds |

## Closeout

- `M8` now has direct regression coverage and a working editable-install baseline.
