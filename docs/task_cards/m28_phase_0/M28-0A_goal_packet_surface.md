# M28-0A Goal Packet Surface

Status: completed

## Goal

Create a single goal packet that combines preset suggestions, plan graph, capability policy preview, and matched descriptors/health for operator review.

## Result

- goal packet preview landed for CLI/API
- governed launch payloads now include the packet so natural-language control surfaces can reuse one canonical preflight shape
