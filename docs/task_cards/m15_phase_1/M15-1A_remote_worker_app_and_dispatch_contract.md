# M15-1A Remote Worker App And Dispatch Contract

- Goal: add the remote worker app and its dispatch endpoint.
- Write set: `apps/remote_worker_api/main.py`.
- Acceptance:
  - remote worker app can execute `shell` and `opencode`
  - dispatch returns execution target, renewal payloads, and execution result
