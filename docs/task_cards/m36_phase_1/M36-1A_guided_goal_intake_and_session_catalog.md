# M36-1A Guided Goal Intake And Session Catalog

Status: completed

## Goal

Replace the minimum workbench preview form with a richer guided intake that can capture the same session context already supported by the interaction contracts.

## Acceptance

- allow workbench creation of sessions with constraints, assumptions, artifact refs, and follow-up context
- surface recent sessions from the same workbench page
- keep the interaction-session contract additive

## Result

- expanded `/ui/workbench` preview intake to accept richer session context
- surfaced recent sessions directly in the workbench
- preserved additive interaction-session semantics and route structure
