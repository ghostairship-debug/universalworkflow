# M34-0A Phase Opening And Focus Freeze

Status: completed

## Goal

Open `M34 Phase 0` formally and freeze the next bounded cleanup line around the structural debts that most directly block a cleaner service-boundary and scheduler-authority interior story.

## Acceptance

- add the `M34` phase doc
- add the `M34` task-card index and detailed cards
- update the current workflow guide so `M34 Phase 0` is the active bounded phase
- update README and the debt registry so the active focus is visible in repository truth
- keep `TD-STRUCT-005` and `TD-STRUCT-006` explicitly deferred while `M34 Phase 0` focuses on `TD-STRUCT-001` and `TD-STRUCT-003`

## Notes

- do not start later breadth work under `M34` naming until the cleanup focus is frozen explicitly
- bug-first still outranks refactor scope

## Result

- opened `M34 Phase 0` formally through the phase doc and task-card pack
- updated the living workflow guide and README so the repository now points at an active post-`M33` bounded phase
- updated the technical debt registry so the active target debts now map to `M34 Phase 0`
- kept `TD-STRUCT-005` and `TD-STRUCT-006` deferred to a post-`M34 Phase 0` bounded follow-on
