# 下一阶段开发计划

> 修订日期：2026-04-21  
> 依据文档：`EVALUATION_REPORT.md`、`README.md`、`docs/current_development_workflow.md`、`docs/reviews/m20-freeze-review.md`、`docs/tech-debt-registry.md`  
> 现实校验：`pytest -q` 本地复核通过，结果为 `264 passed`

## 1. 重新定义短中长期

这份计划不再把短中长期理解为“最近要做什么、再之后做什么”，而是把它理解为产品形态的三个层级：

- 短期：把当前 `M20 / v1 core complete` 基线升级成一个可持续扩展的 agent workflow control plane
- 中期：把产品从“可控执行系统”推进成“受控自治的单项目/单仓多角色协作系统”
- 长期：把产品推进成“通用多智能体工程操作系统”，能够围绕自然语言目标自主组织协作、接入外部能力、持续自我优化并处理任意工程交付

也就是说：

- 短期关注的是底座是否能安全扩张
- 中期关注的是系统是否已经具备真正可用的自治协作能力
- 长期关注的是产品最终要变成什么

## 2. 当前基线判断

项目当前仍应被视为 `M20 / v1 core complete`，下一步的正式入口仍然是 `M21 Phase 0 - Post-M20 Rebaseline And Expansion Freeze`。在这个前提下，需要同时承认两类事实：

1. 当前仓库的核心控制面已经很强，生命周期、治理、审计、回放、远程 worker、调度器权威这些基线已经成立。
2. 当前产品距离“未来形态”还很远，尤其是在多 agent 协作、自主规划、自然语言控制、外部能力编排、自我迭代升级这些高阶能力上，还没有进入真正的产品阶段。

因此后续路线不能只围绕代码整洁度或局部产品化，而要明确朝着未来产品形态收敛。

## 3. 短期计划

时间窗建议：未来 `1-2` 个 sprint  
产品定位：**可扩张的控制面底座**

### 短期目标

短期不是要把产品做成“更花哨的 v1”，而是要把它做成未来多 agent 系统能够安全长出来的底座。也就是说，短期的成功标准不是多加几个功能，而是让后续的自治、协作、外部接入、产品化升级都有稳定承载点。

### 核心任务

1. 开启 `M21 Phase 0`，完成重基线与扩张冻结
   - 用 phase doc、task cards、非目标定义把后续广度工作锁进清晰边界
   - 避免在尚未重基线前直接叠加多 agent、Provider、UI 等新广度

2. 明确核心控制面的演进边界
   - 对 `services.py` 输出结构决策，而不是继续自然膨胀
   - 明确未来哪些能力必须独立成服务边界：编排控制、外部能力网关、worker 协调、记忆检索、策略决策

3. 补齐基础工程治理
   - 把迁移能力从“存在 SQL 文件”提升到“存在明确策略、入口和验证”
   - 建立覆盖率门控、CI 分层验证、关键离线验证常态化
   - 修复当前开发体验问题，例如 Windows 下 pytest 临时目录清理权限异常

4. 为未来能力预留稳定接口
   - 清理 `compile/recompile` 重复逻辑
   - 明确适配器协议、仓储聚合入口、能力网关抽象
   - 让未来接入 agent planner、tool router、external capability source 时不必反复改动主干生命周期

### 短期完成标志

- `M21 Phase 0` 被正式打开并有完整治理资产
- 控制面边界有明确设计结论
- 迁移、测试、CI、验证形成可依赖的工程底座
- 后续广度工作不再直接堆进单一 facade 或单一文件

## 4. 中期计划

时间窗建议：`M21` 后续数个 phase  
产品定位：**受控自治的单项目/单仓多角色协作系统**

### 中期目标

中期的目标不是单纯“做更多入口”或“把 UI 做现代化”，而是让产品第一次具备真正可用的自治协作能力：用户给出自然语言目标，系统能在单个项目边界内进行角色拆分、计划生成、任务执行、审查回流、外部工具调用和人类确认。

这是产品从“workflow runtime”向“工程协作代理系统”跃迁的阶段。

### 中期能力形态

1. 多角色协作初型
   - 不再只是固定模板式 `planner -> coder/researcher -> reviewer`
   - 能根据目标动态生成角色集合、任务依赖、并行批次和审查节点
   - 支持 controller 视角下的任务分派、进度汇总、失败回流和重试策略

2. 自然语言驱动的操作层
   - 用户不必先理解所有 preset / task-kind / adapter 细节
   - 系统能把自然语言目标转译为约束化执行计划，并向用户解释将要做什么、风险是什么、哪里需要确认

3. 外部能力接入层
   - MCP、CLI 工具、远程 worker、Provider 能力不再是零散 opt-in 入口
   - 需要被纳入统一 capability plane，支持发现、选择、调用、失败诊断和权限边界

4. 人机协作闭环
   - 审批、拒绝、补充上下文、纠偏指令、运行中干预都应成为一等能力
   - Web UI 现代化的意义在这里才成立：它服务于协作控制，而不是只服务于展示

5. 受控自治而非无边界自治
   - 中期仍然强调 bounded execution
   - 系统要能自主推进，但不能绕过治理、审计、证据和人工决策边界

### 中期完成标志

- 自然语言目标可稳定转成结构化协作计划
- 多角色协作不再依赖硬编码固定流程
- 外部能力接入成为统一产品能力，而不是散点集成
- 人类能通过自然方式介入、纠偏、批准和接管

## 5. 长期计划

时间窗建议：`M21+` 之后的未来产品形态  
产品定位：**通用多智能体工程操作系统**

### 长期目标

长期计划应围绕产品未来要成为什么，而不是近期实现清单。这个产品的长期形态不应只是一个“更强的工作流引擎”，而应是一个能围绕自然语言意图持续组织多智能体协作、执行复杂工程流程、接入外部世界、进行自我升级和任意工程开发的通用工程操作系统。

### 长期产品能力

1. 多 agent 协作成为核心范式
   - 系统能够围绕一个高层目标自主生成 agent 团队
   - agent 之间具备显式角色、职责边界、通信协议、共享记忆、冲突仲裁和结果合并机制
   - 协作不局限于 planner/coder/reviewer，而是能扩展到架构师、测试者、运维、研究员、文档员、集成代理等多角色网络

2. 自动化流程编排成为内建能力
   - 能把复杂工程任务分解为长期运行的自动化流程
   - 支持事件驱动、定时驱动、条件驱动、人工触发混合编排
   - 支持从需求分析、方案设计、实现、测试、部署、回归、复盘到再规划的全链路流程化

3. 外部能力接入成为生态层
   - 不只是接几个 Provider，而是形成通用外部能力接入框架
   - 能统一接入模型、工具、服务、知识库、代码仓、CI/CD、设计系统、文档系统、测试环境、云资源
   - 能处理权限、可信度、成本、延迟、可用性和失败回退策略

4. 自我迭代升级成为系统特征
   - 系统能够基于历史运行数据、失败案例、人工反馈、交付结果自动总结改进点
   - 能调整自己的计划策略、角色协作方式、工具选择、质量门控和默认执行路线
   - 更进一步，能够在受控边界内升级自己的 workflow 定义、domain pack、skill/export、prompt/policy 资产

5. 任意工程开发成为默认目标域
   - 不应只服务于当前仓库或固定软件交付流
   - 应能迁移到不同工程对象：后端、前端、数据、AI、自动化脚本、运维、文档、研究、集成项目
   - 真正的长期目标是把“工程交付”抽象成可被多 agent 系统通用处理的目标空间

6. 自然语言交互成为主控制面
   - 用户主要通过自然语言表达目标、约束、偏好、风险边界和纠偏意见
   - 系统能持续对话式澄清、汇报、解释计划、申请授权、展示风险、请求决策
   - CLI、API、Web UI 在长期都应成为自然语言控制面的不同承载，而不是互相割裂的入口

7. 人机共治而不是完全无人化
   - 长期不是追求“把人拿掉”，而是追求“把人从低价值协调中拿掉”
   - 人类负责目标定义、边界授权、关键判断和最终责任
   - 系统负责组织、执行、汇报、回放、优化和规模化协作

### 长期架构要求

要支撑上述未来形态，后续架构演进至少要满足以下方向：

- 控制面、执行面、能力面、记忆面、策略面进一步解耦
- agent 间通信、共享状态、任务图、证据链、审计链成为一等对象
- 自然语言目标到结构化计划的转换链路可持续优化
- 外部能力调用从“集成点”升级为“生态框架”
- 自我改进必须带有严格治理、可回滚、可审计和人工否决机制

### 长期完成标志

- 用户可以直接用自然语言发起复杂工程目标
- 系统能自主组建 agent 团队并持续协作完成任务
- 自动化流程能跨阶段长期运行并根据反馈自调优
- 外部能力接入已经成为稳定生态层
- 系统具备受控的自我优化和升级能力
- 产品不再只是一个仓库内 runtime，而是一个通用工程协作操作系统

## 6. 推荐主线

基于上面的分层，推荐路线应当改成：

`短期：打稳可扩张底座` -> `中期：建立受控自治协作能力` -> `长期：收敛到通用多智能体工程操作系统`

其中：

1. 短期必须严格收敛，先解决能不能安全长的问题
2. 中期要开始真正进入“产品能力跃迁”，而不是只做工程修补
3. 长期要始终围绕未来产品形态校准，而不是把长期写成中期功能清单

## 7. 一句话结论

下一阶段计划的核心不应再被理解为“先修一些工程问题、再补一些功能”，而应被理解为：先把当前系统打造成可持续扩张的控制面底座，再把它推进成受控自治的多角色协作系统，最终收敛为一个面向任意工程开发、以自然语言为主控制面、具备多 agent 协作、自动化流程、外部能力生态和受控自我升级能力的通用工程操作系统。
